# FTMS — Financial Transactions Management System

A system of record for financial transactions. Records are never physically deleted:
every transaction carries a status, and deletion is a transition to `Inactive`.

Full onboarding instructions land in phase 7. Architecture lives in [docs/](docs/).
