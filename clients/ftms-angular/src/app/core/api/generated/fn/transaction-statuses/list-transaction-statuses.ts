/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TransactionStatusDto } from '../../models/transaction-status-dto';

export interface ListTransactionStatuses$Params {
}

export function listTransactionStatuses(http: HttpClient, rootUrl: string, params?: ListTransactionStatuses$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TransactionStatusDto>>> {
  const rb = new RequestBuilder(rootUrl, listTransactionStatuses.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TransactionStatusDto>>;
    })
  );
}

listTransactionStatuses.PATH = '/api/transactionstatuses';
